# C语言作用域

代码块作用域、函数作用域、文件作用域

例子：
```c++
#include <stdio.h>

int* fun() {
    int a[] = {34,29,70,99};
    return a;
}

int main() {
    int* p = fun();
    
    for (int i = 0; i < 4; i++) printf("%d",*(p + i));
    
    return 0;
}
```

在上面的例子中， `fun` 函数里面有一个 `a` 数组，但是是函数作用域，调用后就销毁，所以 `main` 函数里的 `p` 变量根本拿不到 `a` 数组

## `auto` 自动局部变量

平常写的 `int a;` 就是 `auto int a;`

## `static` 静态局部变量

- 作用域：在代码块或者定义的函数内有效
- 生命周期：和**整个程序运行周期**一样

如果没有初始赋值，会自动赋值为 0

例子：
```c++
#include <stdio.h>

void fun() {
    static int a = 5;
    printf("%d\n",a);
    a++;
}

int main() {
    fun();
    fun();
    fun();
    
    return 0;
}
```

在上面的例子中，`a` 会递增，因为使用了 `static` 修饰。

而在第一个例子中，若想在外部拿到 `fun` 函数里的 `a` 数组，只需在前加个 `static`。


## 全局变量

- 在函数外定义，可被本文件和其他文件中的函数所共用
- 生命周期和整个程序运行周期一样
- 不同文件的全局变量不可重名


## 静态全局变量

- 在函数外定义，作用范围被限定在所定义的文件中
- 生命周期和整个程序运行周期一样
- 不同文件的静态全局变量可以重名，作用域不冲突