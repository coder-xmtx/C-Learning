# 文件操作

## 文件指针

C语言中有三个特殊的文件指针由系统默认打开，用户无需定义可直接使用：
- stdin：标准输入，默认为当前终端(键盘），使用的`scanf`、`getchar`函数默认从此终端获得数据。
- stdout：标准输出，默认为当前终端(屏幕)，使用`printf`、`puts`函数默认输出信息到此终端。
- stderr：标准错误输出，默认为当前终端（屏幕），使用`perror`函数默认输出信息到此终端。


## 文件读写模式

| 打开模式     | 描述                                     |
|----------|----------------------------------------|
| r 或 rb   | 以只读方式打开一个文本文件（不自动创建文件，若文件不存在则报错）       |
| w 或 wb   | 以写方式打开文件（若文件存在则清空文件，文件不存在则创建一个文件）      |
| a 或 ab   | 以追加方式打开文件，在末尾添加内容，若文件不存在则创建文件          |
| r+ 或 rb+ | 以可读、可写方式打开文件（不创建新文件）                   |
| w+ 或 wb+ | 以可读、可写方式打开文件（如果文件存在则清空文件，文件不存在则创建一个文件） |
| a+ 或 ab+ | 以可读、可写方式打开文件，如果写，同 a                   |


## 打开和关闭文件

- `fopen` 打开文件

函数为 `FILE* fopen(const char* filename, const char* mode)`，其中 mode 是文件读写方式

```c++
FILE* file = fopen("./T02-FileRead.txt", "r")
```

- `fclose` 关闭文件

```c++
fclose(file)
```

## 按字符读取文件

- `fgetc` 按字符读文件，失败返回-1

- `feof` 判断文件结尾，已经到文件结尾返回非 0 值，没有到文件结尾返回 0

> 或者可以用 `(ch = fgetc(file)) != EOF` 来判定没读到结尾，EOF 为 End of File

```c++
#include <stdio.h>
#include <stdlib.h>

int main() {

    FILE* file = fopen("./T02-FileRead.txt", "r");

    if (file == NULL) {
        perror("File Error");
    }

    char ch;

    while (feof(file) != 0 ) {
        ch = fgetc(file)
        printf("%c", ch);
    }

    fclose(file);

    return 0;
}
```

## 按字符写入文件

- `fputc` 按字符写入文件，成功返回写入文件的字符，失败返回 -1

```c++
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main() {

    FILE* file = fopen("T03-FileWrite.txt", "w");

    const char str[] = "hello admin !";

    const int len = strlen(str);

    for(int i = 0; i < len; i++) {
        fputc(str[i], file); // 把 str 的字符一个一个往 file 里面放
    }

    fclose(file);

    return 0;
}
```

例子：将 T02 的内容复制粘贴到 T04

```c++
#include <stdio.h>

int main() {

    FILE* fin = fopen("T02-FileRead.txt", "r");
    FILE* fout = fopen("T04-FileExample.txt", "w");

    while (!feof(fin)) fputc(fgetc(fin), fout);

    fclose(fin);
    fclose(fout);

    return 0;
}
```


## 按行读取与写入文件

- `fputs` 按行写入文件

```c++
#include <stdio.h>

int main() {

    FILE* file = fopen("T04-FileExample.txt", "w");

    char* str[] = {"Hello\n", "World!\n", "Programming\n"};

    for (int i = 0; i < 3; i++) {
        fputs(str[i], file);
    }

    return 0;
}
```

- `fgets` 按行读取文件

```c++
#include <stdio.h>

int main() {

    FILE* file = fopen("T04-FileExample.txt", "r");

    char str[100];

    while (!feof(file)) {
        fgets(str, sizeof(str), file);
        printf("%s\n", str);
    }

    fclose(file);

    return 0;
}
```

## 提取与写入格式化数据

- `sscanf` 提取字符串中的格式化内容

```c++
#include <stdio.h>

int main() {

    const char str[50] = "10-5.5";
    int a;
    double b;
    sscanf(str, "%d-%lf", &a, &b);
    printf("a=%d b=%lf\n", a,b);

    return 0;
}
```

- `sprintf` 将格式化的数据写入字符串中

```c++
#include <stdio.h>

int main() {

    char str[50];
    int a = 5;
    double b = 3.14;
    sprintf(str,"Integer: %d, Double: %lf", a, b );
    printf("%s", str);

    return 0;
}
```